# HomeIotManager - エージェント & 開発者向けガイド (AGENTS.md)

本ドキュメントは、HomeIotManager リポジトリに対して開発、コード変更、レビュー、計画策定を行うエージェントおよび開発者向けの包括的なガイドラインです。

---

## 1. 基本原則 (Prerequisites)

- **言語**:
  - 対話、コミットメッセージ、PR タイトル/本文、レビューコメント、ソースコード上のコメントやドキュメントは原則として **日本語** で記述してください。
- **セキュリティ (Public リポジトリ)**:
  - 本リポジトリは GitHub パブリックリポジトリです。
  - **機密情報（DB パスワード、各 API トークン、IFTTT キー、Hue ユーザー名、宅内 IP アドレス等）をコードやコミット履歴に一切含めないでください**。
  - 機密情報はデプロイ基盤（Jenkins）および環境変数から注入し、リポジトリ内にはダミー値を入れた `.env.example` のみを配置します。

---

## 2. プロジェクト概要 & 技術スタック

**HomeIotManager** は、在宅・外出の自動判定、スマート家電（Philips Hue、ルンバ、シーリングライト）の自動制御、および入退室ログの保存を行う常駐型 IoT 連携システムです（`in_out_log` の刷新版）。

### 技術スタック
- **言語 / ランタイム**: Python 3.12
- **Web フレームワーク**: Flask, Gunicorn
- **データベース**: MySQL (mysql-connector-python)
- **コンテナ / デプロイ**: Podman (Pod / `podman play kube`), Jenkins
- **コード品質 / テスト**:
  - `tox`: 環境分離・テストランナー
  - `flake8`: コード規約チェック（`flake8-quotes`, `flake8-import-order`, `pep8-naming`）
  - `coverage.py`: ユニットテストカバレッジ計測
  - `GitHub Actions`: CI 自動化（PR / main push）

---

## 3. ドキュメント管理方針 (Documentation Policy)

プロジェクトの仕様・計画・履歴は以下の3つの役割に明確に分離して管理します。

```
HomeIotManager/
├── AGENTS.md                                     # 本ガイド（開発規約・ワークフロー）
└── docs/
    ├── spec.md                                   # システム要件定義書 (要件・目的・機能要件)
    ├── architecture.md                           # システムアーキテクチャ・仕様書の正本
    ├── work_history.md                           # 作業履歴 (Work History) の一覧
    └── plans/
        └── YYYY-MM-DD-<overview>.md              # 改修案件ごとの個別計画書アーカイブ
```

1. **要件定義書 ([`docs/spec.md`](docs/spec.md))**:
   - システムの概要・目的および各機能要件（在宅/外出自動判定、スマート家電制御、ルンバ稼働抑制・Web UI、入退室ログ記録）を整理して記述します。
2. **仕様の正本 ([`docs/architecture.md`](docs/architecture.md))**:
   - システム全体のアーキテクチャ図、Podman コンテナ設計、DB スキーマ、外部 API 仕様、設定・定数一覧などの **最新の正式仕様** を記述します。
   - 機能追加やアーキテクチャ変更が発生した場合は、必ず本ドキュメントを同期して更新してください。
3. **改修案件ごとの個別計画書 ([`docs/plans/`](docs/plans/))**:
   - 新規機能や大規模改修に着手する際は、着手前に `docs/plans/YYYY-MM-DD-<overview>.md`（例: `docs/plans/2026-09-20-migration-from-in-out-log.md`）を作成します。
   - 案件の背景、詳細設計、PR 分割計画、検証手順を記録し、完了後も恒久的な計画アーカイブとして保持します。
4. **作業履歴マスター ([`docs/work_history.md`](docs/work_history.md))**:
   - `docs/work_history.md` は **純粋な Work History（案件履歴一覧）** として運用します。
   - 案件の起票・進行・完了時に、日付・案件名・ステータス・計画書へのリンクを記録します（ActivePlan などの重複した詳細は記載せず、個別計画書に委ねます）。

---

## 4. 開発 & PR ワークフロー

### 4.1 PR の細かな分離 (Fine-grained PRs)
- レビュー負荷を軽減し、手戻りを防ぐため、**一度に大きな変更を行わず、論理的単位で PR を細かく分離して提出** してください。
- 計画書（`docs/plans/`）で定義された PR 分割計画に沿って、1 つずつ確実に実装・テスト・マージを行います。

### 4.2 ローカル検証手順
変更を行った際は、PR 作成前に必ず `tox` による検証をローカルで実行してください：

```bash
# 1. リント・スタイルチェック
$ tox -e lint

# 2. 単体テスト・カバレッジチェック
$ tox -e unittest
```

---

## 5. コーディング規約 (Coding Standards)

- **クオートスタイル**:
  - 文字列リテラルには原則として **シングルクオート (`'`)** を使用してください（`flake8-quotes` で強制）。
- **最大行長**:
  - 1 行あたり最大 **140 文字**（flake8 設定に準拠）。
- **インポート順序**:
  - 標準ライブラリ → サードパーティ → プロジェクト内部モジュール の順にソートし、1 行空けてグループ化してください（`smarkets` スタイル）。
- **ファイル命名規則**:
  - Python ファイルはすべてスネークケース（`snake_case.py`）で記述し、`scripts/check_filenames.py` による検証を通過させてください。
- **データベースアクセス**:
  - SQL クエリ発行時は文字列結合やフォーマット文字列（`f"..."` / `format()`）を禁止し、**プレースホルダによるパラメータバインド** を徹底してください。
- **依存ライブラリのバージョン固定**:
  - `requirements/` 配下のファイル（例: `requirements.txt`）で追加するサードパーティライブラリは、メジャーバージョンの破壊的変更を防ぐため、メジャーバージョンを上限として固定（例: `requests>=2.31.0,<3.0.0`）してください。
- **HTTP 通信とコネクション再利用**:
  - 本システムは常駐バッチアプリケーションであるため、HTTP 通信（Philips Hue、IFTTT 等への API リクエスト）を行う際は `requests.Session` などを用いて可能な限りコネクションを再利用し、処理のオーバーヘッドを削減してください。
- **設定値の集約**:
  - 動作パラメータ・しきい値・ポート番号などは `homeiot/constants.py` の 1 箇所に集約し、各モジュールに数値をハードコードしないでください。
